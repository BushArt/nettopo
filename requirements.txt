# NetTopo Python Dependencies
# Install: pip install -r requirements.txt
# Phase 1 — Foundation

pyyaml>=6.0            # config.yaml parsing
rich>=13.0             # pretty terminal output during scanning
pytest>=8.0            # test framework

# Phase 2 additions (install now to prepare):
websockets>=12.0
aiohttp>=3.9
pytest-asyncio>=0.23
